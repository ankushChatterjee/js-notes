<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>JS Notes</title>
    <link href="https://fonts.googleapis.com/css?family=Nothing+You+Could+Do|Varela" rel="stylesheet">
    <style>     
                html{
                    padding:0px;
                }
                body{
                    padding:0px;
                    margin:0px;
                    background:#e9e9e9;
                }
                pre{
                    overflow-x:scroll;
                    background:#1e1e1e;
                    padding:1em;
                    border-radius:5px;
                    font-size:150%;
                }
                he{
                    margin:0px;
                    padding:0px;
                }
                h4{
                    color:#BDC3C7;
                    margin:0px;
                    padding:0px;
                }
                a{
                    text-decoration:none;
                }

                span:hover{
                    text-decoration:underline;
                }
                span{
                    display:inline-block;
                }
                span .tooltip {
                    display:none;
                    width: 120px;
                    background-color: #6C7A89;
                    color: #fff;
                    text-align: center;
                    border-radius: 6px;
                    padding: 5px 0;
                    margin:1.5em;
                
                    /* Position the tooltip */
                    position: absolute;
                    z-index: 1;
                }
                
                span:hover .tooltip {
                    display:inline;
                    visibility: visible;
                }
                header{
                    text-align:center;
                    background:#F5AB35;
                    padding:1em;
                }
                header .title{
                    font-family: 'Nothing You Could Do', cursive;
                    font-weight: bold;
                    font-size:3em;
                }
                header .author{
                    font-family: 'Nothing You Could Do', cursive;
                    color: black;
                    padding:0px;
                    margin:0.30em;
                }
                .codenotes{
                    padding: 1em;
                }
                .foldername{
                    font-family: 'Varela', sans-serif;
                    color:#F5AB35;
                    background:black;
                    padding:0.5em;
                    border-radius:5px;
                    overflow-x:hidden;
                }
                @media screen and (max-width: 600px) {
                    .foldername{
                        overflow-x:scroll;
                    }
                }
            </style>
</head>
<body>
    <header>
        <h1 class="title">JS Notes</h1>
        <h2 class="author">Project : <%= project %></h2>
        <h2 class="author">by <%= author %></h2>
    </header>
    <div class="divider"></div>
    <div class="codenotes">
        <% shinfo.forEach(function(info){ %>
            <h2 class="foldername"><%= info.folderName %></h2>
            <% info.filesMarkup.forEach(function(note){ %>
                <div class="note">
                    <% Object.keys(note).forEach(function(fun){ %>
                        <%- note[fun].markup %>
                    <% }); %>
                </div>
            <% }); %>
        <% }); %>
    </div>
</body>
</html>